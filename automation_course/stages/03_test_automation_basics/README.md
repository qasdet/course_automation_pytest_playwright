# Модуль 3: Основы автоматизации тестирования

## 🎯 Цели модуля (3 недели / 12 занятий)

**По окончании модуля студент сможет:**
- Понимать принципы автоматизированного тестирования
- Настраивать тестовое окружение для автоматизации
- Писать простые автоматизированные тесты
- Работать с основными инструментами автоматизации
- Понимать архитектуру тестовых фреймворков

## 👨‍🏫 Методические материалы для преподавателя

### Общие рекомендации по преподаванию автоматизации:

**🎯 Ключевые принципы преподавания:**
- **От простого к сложному:** Начинать с базовых концепций, постепенно увеличивая сложность
- **Практико-ориентированность:** Каждая теория сразу закрепляется практикой
- **Реальные примеры:** Использовать примеры из production, а не искусственные задачи
- **Индивидуальный подход:** Учитывать разный уровень подготовки студентов

**📋 Технические требования для преподавателя:**
- Компьютер с Python 3.11+, установленными библиотеками
- Доступ к различным веб-сайтам для тестирования
- Подготовленные примеры кода и тестовые стенды
- Демонстрационные виртуальные машины с разными ОС

**⏰ Структура занятий по автоматизации:**
- 10 мин: Проверка домашнего задания + разбор проблем
- 20 мин: Новая теория с демонстрациями
- 30 мин: Совместное программирование (live coding)
- 20 мин: Самостоятельная практика студентов
- 10 мин: Подведение итогов + домашнее задание

### 🔧 Подготовка учебной среды:

**Для студентов необходимо подготовить:**
```bash
# requirements.txt для модуля автоматизации
pytest==7.4.3
pytest-asyncio==0.21.1
playwright==1.40.0
pytest-playwright==0.4.3
python-dotenv==1.0.0
requests==2.31.0
allure-pytest==2.13.2
black==23.11.0
flake8==6.1.0
```

**Директория проекта для студентов:**
```
automation_course/
├── tests/
│   ├── unit/
│   ├── integration/
│   └── ui/
├── pages/
├── fixtures/
├── utils/
├── config/
├── reports/
├── conftest.py
├── .env.example
└── requirements.txt
```

**Подготовленные тестовые стенды:**
- Простые веб-сайты для UI тестирования
- Mock API серверы
- Тестовые базы данных
- Примеры с преднамеренными багами

## 📚 Теоретические основы автоматизации

### Что такое автоматизация тестирования: Глубокое погружение

#### Определение и ключевые концепции

**Автоматизация тестирования** - это использование специализированных инструментов и скриптов для выполнения тестов без ручного участия человека.

```python
# Пример: Ручное vs автоматизированное тестирование

class ManualTestingExample:
    def test_login_manually(self):
        """
        РУЧНОЕ ТЕСТИРОВАНИЕ (что делает человек):
        1. Открыть браузер
        2. Перейти на сайт
        3. Найти поле email
        4. Ввести email
        5. Найти поле пароля
        6. Ввести пароль
        7. Найти кнопку входа
        8. Кликнуть по кнопке
        9. Проверить результат
        10. Записать результат в отчет
        Время: ~2 минуты
        """
        pass

class AutomatedTestingExample:
    def test_login_automated(self):
        """
        АВТОМАТИЗИРОВАННОЕ ТЕСТИРОВАНИЕ:
        """
        # Весь процесс в 5 строках кода
        page.goto("https://example.com/login")
        page.fill("#email", "user@example.com")
        page.fill("#password", "password123")
        page.click("#login-button")
        assert page.url == "https://example.com/dashboard"
        
        # Время выполнения: ~2 секунды
        # Может выполняться 1000 раз без усталости

# ROI сравнение:
manual_test_time = 2 * 60  # 2 минуты в секундах
automated_test_time = 2    # 2 секунды
setup_time = 30 * 60       # 30 минут на написание теста
runs_needed = 100          # Сколько раз будет запускаться

manual_total = manual_test_time * runs_needed  # 12,000 секунд (~3.3 часа)
automated_total = setup_time + (automated_test_time * runs_needed)  # 2,000 секунд (~33 минуты)

savings = manual_total - automated_total  # 10,000 секунд (~2.8 часа экономии)
```

#### Когда применять автоматизацию: Подробное руководство

```python
# Критерии для принятия решения об автоматизации

class AutomationDecisionMatrix:
    def __init__(self):
        self.positive_factors = []
        self.negative_factors = []
        self.neutral_factors = []
    
    def evaluate_test_case(self, test_info):
        """
        Оценка тест-кейса для автоматизации
        """
        score = 0
        
        # ЧАСТОТА ВЫПОЛНЕНИЯ (+10 за каждое совпадение)
        frequency_factors = [
            (test_info.frequency == "daily", "Ежедневное выполнение"),
            (test_info.frequency == "every_commit", "При каждом коммите"),
            (test_info.frequency == "weekly", "Еженедельное выполнение"),
            (test_info.part_of_regression, "Часть регрессионного набора")
        ]
        
        for condition, description in frequency_factors:
            if condition:
                self.positive_factors.append(description)
                score += 10
        
        # СЛОЖНОСТЬ И ТОЧНОСТЬ (+8 за каждое)
        complexity_factors = [
            (test_info.requires_precise_timing, "Требует точного тайминга"),
            (test_info.mathematical_calculations, "Математические вычисления"),
            (test_info.large_data_sets, "Работа с большими объемами данных"),
            (test_info.repeatable_exactly, "Должно выполняться идентично")
        ]
        
        for condition, description in complexity_factors:
            if condition:
                self.positive_factors.append(description)
                score += 8
        
        # КРИТИЧНОСТЬ (+15 за критическую функциональность)
        if test_info.business_critical:
            self.positive_factors.append("Критическая бизнес-функциональность")
            score += 15
        
        # НЕГАТИВНЫЕ ФАКТОРЫ (-5 за каждое)
        negative_factors = [
            (test_info.requires_human_judgment, "Требует человеческого суждения"),
            (test_info.exploratory_nature, "Исследовательский характер"),
            (test_info.one_time_execution, "Одноразовое выполнение"),
            (test_info.ui_changes_frequently, "Интерфейс часто меняется"),
            (test_info.difficult_to_automate, "Сложно автоматизировать")
        ]
        
        for condition, description in negative_factors:
            if condition:
                self.negative_factors.append(description)
                score -= 5
        
        # РЕКОМЕНДАЦИЯ
        if score >= 20:
            return "РЕКОМЕНДУЕТСЯ АВТОМАТИЗАЦИЯ"
        elif score >= 10:
            return "РАССМОТРЕТЬ ВОЗМОЖНОСТЬ АВТОМАТИЗАЦИИ"
        else:
            return "ЛЕГЧЕ ОСТАВИТЬ РУЧНЫМ ТЕСТИРОВАНИЕМ"

# Практические примеры:
examples = [
    {
        "scenario": "Тестирование API endpoint /users",
        "frequency": "every_commit",
        "business_critical": True,
        "requires_human_judgment": False,
        "recommendation": "ОДНОЗНАЧНО АВТОМАТИЗИРОВАТЬ"
    },
    {
        "scenario": "Тестирование нового UX дизайна",
        "frequency": "one_time",
        "business_critical": False,
        "requires_human_judgment": True,
        "recommendation": "ОСТАВИТЬ РУЧНЫМ"
    },
    {
        "scenario": "Регрессионное тестирование формы заказа",
        "frequency": "daily",
        "business_critical": True,
        "ui_changes_frequently": True,
        "recommendation": "АВТОМАТИЗИРОВАТЬ С ОСТОРОЖНОСТЬЮ"
    }
]
```

#### Архитектура автоматизированных тестов

```python
# СТРУКТУРА ПРОЕКТА АВТОМАТИЗАЦИИ

class TestArchitecture:
    def __init__(self):
        self.layers = {}
    
    def describe_layered_architecture(self):
        """
        Многоуровневая архитектура тестов
        """
        layers = {
            "Presentation Layer (UI Tests)": {
                "purpose": "Тестирование пользовательского интерфейса",
                "tools": ["Playwright", "Selenium", "Cypress"],
                "examples": ["Login form testing", "Navigation testing", "Form validation"],
                "characteristics": "Медленные, хрупкие, но важные для UX"
            },
            
            "Service Layer (API Tests)": {
                "purpose": "Тестирование API и бизнес-логики",
                "tools": ["requests", "pytest", "Postman/Newman"],
                "examples": ["REST API endpoints", "GraphQL queries", "Authentication"],
                "characteristics": "Быстрые, стабильные, хорошее покрытие бизнес-логики"
            },
            
            "Unit Layer (Unit Tests)": {
                "purpose": "Тестирование отдельных функций/классов",
                "tools": ["pytest", "unittest", "coverage.py"],
                "examples": ["Calculator functions", "Data processing", "Validation logic"],
                "characteristics": "Очень быстрые, точные, раннее обнаружение багов"
            }
        }
        
        return layers
    
    def testing_pyramid(self):
        """
        ПИРАМИДА ТЕСТИРОВАНИЯ - оптимальное распределение тестов
        """
        pyramid = {
            "BASE - Unit Tests": {
                "percentage": "70%",
                "speed": "< 1 second",
                "cost": "Low",
                "stability": "High"
            },
            
            "MIDDLE - Service/API Tests": {
                "percentage": "20%",
                "speed": "1-10 seconds",
                "cost": "Medium",
                "stability": "Medium"
            },
            
            "TOP - UI Tests": {
                "percentage": "10%",
                "speed": "10+ seconds",
                "cost": "High",
                "stability": "Low"
            }
        }
        
        return pyramid

# Практическая реализация архитектуры

class TestProjectStructure:
    def __init__(self):
        self.structure = {
            "root/": {
                "tests/": {
                    "unit/": ["test_calculations.py", "test_validators.py"],
                    "integration/": ["test_api_endpoints.py", "test_database.py"],
                    "ui/": ["test_login_page.py", "test_checkout_flow.py"]
                },
                "pages/": ["LoginPage.py", "DashboardPage.py", "BasePage.py"],
                "api/": ["UserService.py", "OrderService.py"],
                "utils/": ["TestDataGenerator.py", "AssertionsHelper.py"],
                "config/": ["test_config.py", ".env"],
                "reports/": ["html_reports/", "xml_results/", "screenshots/"],
                "conftest.py": "Глобальные фикстуры и настройки pytest",
                "requirements.txt": "Зависимости проекта"
            }
        }
    
    def get_best_practices(self):
        """
        ЛУЧШИЕ ПРАКТИКИ для структуры проекта
        """
        return [
            "Разделяйте тесты по типам (unit/integration/ui)",
            "Используйте Page Object Model для UI тестов",
            "Храните конфигурации отдельно от кода",
            "Генерируйте уникальные тестовые данные",
            "Делайте тесты независимыми друг от друга",
            "Используйте дескриптивные имена тестов",
            "Храните секреты в переменных окружения",
            "Регулярно обновляйте зависимости"
        ]
```

### Тестовые фреймворки: Подробное сравнение

#### Pytest - основной фреймворк курса

```python
# ПОДРОБНАЯ АРХИТЕКТУРА PYTEST

class PytestFramework:
    def __init__(self):
        self.components = {}
    
    def framework_components(self):
        """
        Основные компоненты pytest
        """
        return {
            "Test Runner": {
                "function": "Обнаружение и выполнение тестов",
                "features": ["Автоматическое обнаружение тестов", 
                           "Параллельное выполнение",
                           "Выборочное выполнение тестов",
                           "Генерация отчетов"]
            },
            
            "Assertion Engine": {
                "function": "Проверка результатов тестов",
                "features": ["Rich assertions с подробными сообщениями",
                           "Soft assertions (проверка всех условий)",
                           "Custom assertion helpers"]
            },
            
            "Fixture System": {
                "function": "Подготовка и очистка тестового окружения",
                "features": ["scope (function, class, module, session)",
                           "parametrization",
                           "dependency injection",
                           "autouse fixtures"]
            },
            
            "Plugin Architecture": {
                "function": "Расширяемость фреймворка",
                "popular_plugins": ["pytest-html", "pytest-xdist", 
                                  "pytest-cov", "allure-pytest"]
            }
        }
    
    def advanced_features(self):
        """
        Продвинутые возможности pytest
        """
        return {
            "Markers": [
                "@pytest.mark.slow - для медленных тестов",
                "@pytest.mark.integration - для интеграционных тестов",
                "@pytest.mark.skip - пропустить тест",
                "@pytest.mark.xfail - ожидаемый провал"
            ],
            
            "Parametrization": "Выполнение одного теста с разными данными",
            
            "Hooks": "Точки расширения для кастомизации поведения",
            
            "Configuration": "pytest.ini, pyproject.toml, conftest.py"
        }

# ПРАКТИЧЕСКИЕ ПРИМЕРЫ PYTEST

class PytestExamples:
    def basic_test_structure(self):
        """
        Базовая структура теста
        """
        def test_user_authentication():
            # ARRANGE - Подготовка
            user_service = UserService()
            test_user = {"email": "test@example.com", "password": "password123"}
            
            # ACT - Действие
            result = user_service.authenticate(test_user["email"], test_user["password"])
            
            # ASSERT - Проверка
            assert result.success == True
            assert result.user_id is not None
            assert result.token is not None
    
    def parametrized_test(self):
        """
        Параметризованный тест
        """
        import pytest
        
        @pytest.mark.parametrize("input_a,input_b,expected", [
            (2, 3, 5),           # Тест 1
            (-1, 1, 0),          # Тест 2
            (0, 0, 0),           # Тест 3
            (100, 200, 300),     # Тест 4
            (1.5, 2.5, 4.0),     # Тест 5 с float
        ])
        def test_addition(input_a, input_b, expected):
            calculator = Calculator()
            result = calculator.add(input_a, input_b)
            assert result == expected
    
    def fixture_example(self):
        """
        Пример использования фикстур
        """
        import pytest
        
        @pytest.fixture(scope="session")
        def database_connection():
            """Фикстура уровня сессии - создается один раз"""
            db = create_test_database()
            yield db
            db.cleanup()
        
        @pytest.fixture
        def user_data(database_connection):
            """Фикстура уровня функции"""
            user = {
                "name": "Test User",
                "email": "test@example.com",
                "created_at": datetime.now()
            }
            user_id = database_connection.insert_user(user)
            yield {"id": user_id, "data": user}
            database_connection.delete_user(user_id)
        
        def test_user_creation(user_data, database_connection):
            """Тест использует обе фикстуры"""
            # user_data уже содержит созданного пользователя
            stored_user = database_connection.get_user(user_data["id"])
            assert stored_user["email"] == user_data["data"]["email"]
```

#### Сравнение с другими фреймворками

```python
# СРАВНЕНИЕ ПОПУЛЯРНЫХ ФРЕЙМВОРКОВ

class FrameworkComparison:
    def __init__(self):
        self.frameworks = {
            "pytest": {
                "pros": ["Простой синтаксис", "Богатая экосистема", "Отличная документация",
                        "Гибкие фикстуры", "Мощная параметризация"],
                "cons": ["Может быть избыточным для простых тестов"],
                "best_for": ["Комплексные проекты", "Командная разработка", "CI/CD интеграция"]
            },
            
            "unittest": {
                "pros": ["Встроен в Python", "Знаком многим разработчикам", "Структурированный подход"],
                "cons": ["Более многословный", "Менее гибкие фикстуры", "Старомодный синтаксис"],
                "best_for": ["Простые тесты", "Legacy проекты", "Когда нельзя устанавливать сторонние пакеты"]
            },
            
            "behave (BDD)": {
                "pros": ["Поведенческое тестирование", "Понятно бизнесу", "Gherkin синтаксис"],
                "cons": ["Больше boilerplate кода", "Сложнее для программистов"],
                "best_for": ["Acceptance тесты", "Когда важно понимание бизнесом"]
            }
        }
    
    def why_pytest_for_this_course(self):
        """
        Почему выбран именно pytest для этого курса
        """
        return {
            "learning_curve": "Мягкая кривая обучения для новичков",
            "industry_standard": "Широко используется в индустрии",
            "flexibility": "Подходит как для простых, так и для сложных тестов",
            "community": "Активное сообщество и много ресурсов для обучения",
            "integration": "Отличная интеграция с Playwright и другими инструментами",
            "future_proof": "Навыки будут востребованы на рынке труда"
        }
```

## ❓ Ответы на возможные вопросы студентов

### Вопросы по автоматизации в целом

**Q: Автоматизация - это когда компьютер сам всё тестирует?**

A: Не совсем. Компьютер выполняет заранее написанные сценарии, но:

```python
# Что делает человек:
code_writer_responsibilities = [
    "Анализирует требования",
    "Проектирует тестовые сценарии",
    "Пишет тестовый код",
    "Настраивает тестовое окружение",
    "Анализирует результаты",
    "Поддерживает тесты"
]

# Что делает компьютер:
computer_responsibilities = [
    "Выполняет написанные сценарии",
    "Сравнивает фактические и ожидаемые результаты",
    "Генерирует отчеты",
    "Делает скриншоты при ошибках"
]

# Автоматизация = человек + инструменты
automation_equation = "Человек (мозг) + Инструменты (руки) = Эффективное тестирование"
```

**Q: Сколько времени занимает написание автоматизированных тестов по сравнению с ручным тестированием?**

A: Это зависит от сложности:

```python
# Сравнение времени разработки

time_comparison = {
    "simple_ui_test": {
        "manual": "5 минут на выполнение",
        "automation_development": "2 часа на написание",
        "automation_execution": "10 секунд на выполнение",
        "break_even_point": "24 запуска",
        "recommendation": "Автоматизировать, если будет больше 50 запусков"
    },
    
    "complex_api_test": {
        "manual": "15 минут на выполнение",
        "automation_development": "4 часа на написание",
        "automation_execution": "30 секунд на выполнение",
        "break_even_point": "16 запусков",
        "recommendation": "Автоматизировать почти всегда"
    },
    
    "exploratory_test": {
        "manual": "2 часа исследования",
        "automation_development": "Невозможно автоматизировать",
        "recommendation": "Только ручное тестирование"
    }
}

# Практическое правило:
def calculate_roi(manual_time, automation_time, execution_time):
    """
    Рассчитывает точку окупаемости автоматизации
    """
    # Время на разработку в минутах
    dev_time_minutes = automation_time * 60
    
    # Время ручного теста в минутах
    manual_time_minutes = manual_time
    
    # Точка окупаемости
    break_even_runs = dev_time_minutes / (manual_time_minutes - execution_time)
    
    return {
        "break_even_runs": round(break_even_runs),
        "recommendation": "Автоматизировать" if break_even_runs < 100 else "Оставить ручным"
    }
```

**Q: Можно ли полностью заменить ручных тестировщиков автоматизацией?**

A: Нет, и вот почему:

```python
# То, что НЕ МОЖЕТ автоматизация:
human_only_capabilities = [
    "Интуитивное тестирование",           # Заметить странное поведение
    "Креативное исследование",             # Придумать нестандартные сценарии
    "Эмоциональная оценка UX",             # Как пользователь чувствует интерфейс
    "Адаптация к изменениям",              # Быстро реагировать на новые требования
    "Коммуникация с командой",             # Объяснить сложные проблемы
    "Бизнес-понимание",                    # Оценить влияние изменений на бизнес"
]

# То, что ЛУЧШЕ делает автоматизация:
better_automated = [
    "Регрессионное тестирование",          # Тысячи проверок за минуты
    "Тестирование производительности",     # Нагрузочные тесты
    "Тестирование API",                    # Структурированные запросы
    "Параллельное выполнение",             # Множество тестов одновременно
    "Точность и повторяемость",            # Никаких человеческих ошибок"
]

# Будущее = Сотрудничество
future_of_testing = "Ручные тестировщики станут тест-инженерами, создающими и поддерживающими автоматизацию"
```

### Вопросы по pytest

**Q: Зачем нужны фикстуры? Почему не создавать всё внутри теста?**

A: Фикстуры решают несколько важных проблем:

```python
# ПЛОХОЙ ПОДХОД - всё в тесте
def test_bad_approach():
    # Создание данных
    db = create_database()
    user = create_test_user()
    db.insert(user)
    
    # Тестирование
    result = authenticate(user.email, user.password)
    assert result.success
    
    # Очистка - легко забыть!
    db.delete(user.id)  # Что если тест упадет раньше?

# ХОРОШИЙ ПОДХОД - с фикстурами
@pytest.fixture
def test_database():
    db = create_database()
    yield db
    db.cleanup()  # Автоматически вызывается даже если тест упадет

@pytest.fixture
def test_user(test_database):
    user = create_test_user()
    user_id = test_database.insert(user)
    yield {"id": user_id, "data": user}
    test_database.delete(user_id)

def test_good_approach(test_user, test_database):
    # Только логика теста
    result = authenticate(test_user["data"].email, test_user["data"].password)
    assert result.success
    # Очистка происходит автоматически!

# Преимущества фикстур:
fixture_benefits = [
    "DRY принцип - не повторяем код создания/очистки",
    "Автоматическая очистка даже при падении тестов",
    "Переиспользование между тестами",
    "Управление зависимостями",
    "Разные scope (function, class, module, session)"
]
```

**Q: В чем разница между @pytest.mark.parametrize и обычным циклом?**

A: 

```python
# Обычный цикл - ПЛОХО
def test_with_loop():
    test_cases = [(2, 3, 5), (1, 1, 2), (0, 0, 0)]
    for a, b, expected in test_cases:
        result = add(a, b)
        assert result == expected
    # Проблема: если один случай упадет, остальные не проверятся

# Parametrize - ХОРОШО
@pytest.mark.parametrize("a,b,expected", [
    (2, 3, 5),    # Отдельный тест
    (1, 1, 2),    # Отдельный тест
    (0, 0, 0),    # Отдельный тест
])
def test_addition(a, b, expected):
    result = add(a, b)
    assert result == expected
    # Преимущества:
    # - Каждый случай - отдельный тест
    # - Понятные имена в отчетах
    # - Все случаи выполняются независимо
    # - Легче отлаживать конкретный случай
```

### Вопросы по архитектуре тестов

**Q: Почему нужно следовать пирамиде тестирования?**

A: 

```python
# Пирамида тестирования - экономически обоснованный подход

class TestingPyramidExplanation:
    def __init__(self):
        self.pyramid_levels = [
            {
                "level": "UNIT TESTS (70%)",
                "cost_to_write": "Низкая",
                "execution_speed": "Очень быстрая (< 1 сек)",
                "maintenance_cost": "Низкая",
                "coverage": "Конкретные функции",
                "example": "Тестирование функции расчета скидки"
            },
            {
                "level": "SERVICE TESTS (20%)",
                "cost_to_write": "Средняя",
                "execution_speed": "Быстрая (1-10 сек)",
                "maintenance_cost": "Средняя",
                "coverage": "Интеграция компонентов",
                "example": "Тестирование API авторизации"
            },
            {
                "level": "UI TESTS (10%)",
                "cost_to_write": "Высокая",
                "execution_speed": "Медленная (10+ сек)",
                "maintenance_cost": "Высокая",
                "coverage": "Пользовательский опыт",
                "example": "Тестирование полного сценария покупки"
            }
        ]
    
    def economic_reasoning(self):
        """
        Экономическое обоснование пирамиды
        """
        return {
            "cost_effectiveness": "Больше дешевых тестов, меньше дорогих",
            "feedback_speed": "Быстрые тесты дают быструю обратную связь",
            "maintenance": "Меньше хрупких UI тестов = меньше поддержки",
            "reliability": "Unit тесты самые стабильные"
        }
```

## 📋 Подробный тайминг занятий

### Занятие 3.1: Что такое автоматизация тестирования (90 минут)

**0-10 мин: Введение и мотивация**
- Приветствие и обзор модуля
- Почему автоматизация важна в современной разработке
- Карьерные перспективы автоматизаторов

**10-30 мин: Теория - Основы автоматизации**
- Определение автоматизации тестирования
- ROI автоматизации
- Когда применять автоматизацию
- Архитектура автоматизированных тестов

**30-55 мин: Практика - Живая демонстрация**
- Демонстрация ручного теста
- Демонстрация автоматизированного теста
- Сравнение скорости и надежности
- Показ реальных примеров из практики

**55-75 мин: Интерактивная часть**
- Студенты в группах обсуждают: какие тесты автоматизировать
- Анализируют простое веб-приложение
- Презентация выводов каждой группы

**75-90 мин: Закрепление и домашнее задание**
- Подведение итогов занятия
- Ответы на вопросы
- Назначение домашнего задания
- Анонс следующего занятия

### Занятие 3.2: Тестовые фреймворки (90 минут)

**0-15 мин: Проверка домашнего задания**
- Разбор решений студентов
- Обсуждение возникших трудностей
- Демонстрация лучших практик

**15-40 мин: Теория - Архитектура pytest**
- Компоненты тестового фреймворка
- Сравнение pytest с другими фреймворками
- Основные возможности pytest
- Best practices использования

**40-65 мин: Практика - Работа с pytest**
- Установка и настройка pytest
- Написание первого теста
- Использование assert"ов
- Запуск тестов и анализ результатов

**65-80 мин: Самостоятельная работа**
- Студенты пишут свои первые тесты
- Преподаватель помогает индивидуально
- Работа в парах для peer-review

**80-90 мин: Завершение занятия**
- Обзор пройденного материала
- Демонстрация типичных ошибок
- Домашнее задание

### Занятие 3.3: Настройка рабочего окружения (90 минут)

**0-20 мин: Теория - Подготовка среды**
- Установка Python и виртуальных окружений
- Управление зависимостями
- Структура проекта автоматизации
- Настройка IDE

**20-50 мин: Практика - Hands-on setup**
- Создание структуры проекта
- Установка необходимых библиотек
- Настройка конфигурационных файлов
- Проверка работоспособности окружения

**50-75 мин: Работа с конфигурацией**
- Создание requirements.txt
- Настройка pytest.ini
- Работа с переменными окружения
- Организация тестовых данных

**75-90 мин: Закрепление**
- Проверка установки у каждого студента
- Решение возникших проблем
- Домашнее задание по настройке

### Занятие 3.4: Первый автоматизированный тест (90 минут)

**0-15 мин: Проверка окружения**
- Убедиться, что все установили окружение
- Быстрая проверка конфигурации

**15-40 мин: Теория и практика - Написание тестов**
- Структура автоматизированного теста
- Паттерн AAA (Arrange-Act-Assert)
- Работа с локаторами
- Обработка ожиданий

**40-65 мин: Live coding с Playwright**
- Установка Playwright
- Первый браузерный тест
- Работа с элементами страницы
- Проверка результатов

**65-85 мин: Самостоятельная практика**
- Студенты создают свои тесты
- Тестирование разных веб-сайтов
- Эксперименты с различными сценариями

**85-90 мин: Финал занятия**
- Демонстрация лучших работ студентов
- Ответы на вопросы
- Домашнее задание
- Анонс следующего модуля

## 📚 Содержание модуля

### Неделя 1: Теоретические основы
#### Занятие 3.1: Что такое автоматизация тестирования
**Видео-скрипт (25 минут):**

**Введение в автоматизацию (3 мин)**
> "Сегодня мы поговорим о том, что такое автоматизация тестирования, когда ее стоит применять, и какие инструменты существуют."

**Определение автоматизации (5 мин)**
```
Автоматизация тестирования - это использование специальных инструментов 
и скриптов для выполнения тестов без ручного участия.

ОСНОВНЫЕ ЦЕЛИ:
✓ Повышение эффективности тестирования
✓ Обеспечение повторяемости тестов
✓ Снижение влияния человеческого фактора
✓ Ускорение регрессионного тестирования
✓ Раннее выявление дефектов
```

**Когда применять автоматизацию? (6 мин)**
```python
# Пример: Когда автоматизация ОПРАВДАНА

# ✅ ХОРОШИЕ КАНДИДАТЫ ДЛЯ АВТОМАТИЗАЦИИ:
def should_automate_test(test_type, frequency, complexity):
    reasons_to_automate = []
    
    # Высокая частота выполнения
    if frequency == "daily" or frequency == "every_commit":
        reasons_to_automate.append("Частое выполнение")
    
    # Сложные расчеты
    if complexity == "math_heavy":
        reasons_to_automate.append("Сложные вычисления")
    
    # Регрессионное тестирование
    if test_type == "regression":
        reasons_to_automate.append("Регрессионные проверки")
    
    # Критическая функциональность
    if test_type == "critical_functionality":
        reasons_to_automate.append("Критический функционал")
    
    return len(reasons_to_automate) > 0, reasons_to_automate

# Примеры:
# Тесты API - ✅ Да (часто, точно, быстро)
# Тесты UI - ✅ Да (регрессия, повторяемость)
# Юзабилити - ❌ Нет (требуется человеческий взгляд)
# Исследовательское тестирование - ❌ Нет (творческий подход)
```

**ROI автоматизации (4 мин)**
```
РАСЧЕТ ROI (Return on Investment):

ROI = (Экономия времени - Время на разработку) / Время на разработку × 100%

Пример:
- Ручной тест: 30 минут
- Автоматизированный тест: 2 минуты
- Выполняется 200 раз в месяц
- Время разработки: 8 часов

Экономия в месяц: 200 × (30 - 2) / 60 = 93 часа
ROI за первый месяц: (93 - 8) / 8 × 100% = 1062.5%
```

**Инструменты автоматизации (4 мин)**
```python
# ПОПУЛЯРНЫЕ ИНСТРУМЕНТЫ:

tools = {
    "Python": {
        "frameworks": ["pytest", "unittest"],
        "web_testing": ["Selenium", "Playwright"],
        "api_testing": ["requests", "pytest"],
        "bdd": ["behave", "pytest-bdd"]
    },
    "JavaScript": {
        "frameworks": ["Jest", "Mocha"],
        "web_testing": ["Cypress", "Playwright"],
        "api_testing": ["supertest"]
    },
    "Java": {
        "frameworks": ["JUnit", "TestNG"],
        "web_testing": ["Selenium"]
    }
}

# НАШ ВЫБОР: Python + pytest + Playwright
# Почему?
# - Простота обучения
# - Богатая экосистема
# - Отличная документация
# - Активное сообщество
```

**Практическая демонстрация (3 мин)**
Сравнение ручного и автоматизированного тестирования одного и того же функционала

#### Занятие 3.2: Тестовые фреймворки
**Видео-скрипт (23 минуты):**

**Что такое тестовый фреймворк? (5 мин)**
```python
# ТЕСТОВЫЙ ФРЕЙМВОРК - это набор инструментов и соглашений 
# для организации и выполнения автоматизированных тестов

# Пример: pytest - популярный Python фреймворк

import pytest

# Базовая структура теста
def test_basic_calculation():
    """Простой тест сложения"""
    result = 2 + 3
    assert result == 5

# Тест с параметрами
@pytest.mark.parametrize("a,b,expected", [
    (2, 3, 5),
    (-1, 1, 0),
    (0, 0, 0),
    (100, 200, 300)
])
def test_addition_parametrized(a, b, expected):
    """Параметризованный тест сложения"""
    result = a + b
    assert result == expected

# Тест с фикстурой
@pytest.fixture
def calculator():
    """Фикстура - подготовка тестовых данных"""
    return {
        'add': lambda x, y: x + y,
        'multiply': lambda x, y: x * y
    }

def test_calculator_fixture(calculator):
    """Использование фикстуры"""
    assert calculator['add'](2, 3) == 5
    assert calculator['multiply'](4, 5) == 20
```

**Архитектура тестового фреймворка (6 мин)**
```
АРХИТЕКТУРА PYTEST:

┌─────────────────────────────────────┐
│           Test Runner               │
│     (Запуск и выполнение тестов)    │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│          Test Discovery             │
│      (Поиск тестовых функций)       │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│          Fixture Setup              │
│      (Подготовка тестового окружения)│
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│          Test Execution             │
│         (Выполнение тестов)         │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│          Assertion Engine           │
│        (Проверка результатов)       │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│          Reporting System           │
│       (Генерация отчетов)           │
└─────────────────────────────────────┘
```

**Best Practices фреймворков (7 мин)**
```python
# ЛУЧШИЕ ПРАКТИКИ:

# 1. Именование тестов
def test_user_can_login_with_valid_credentials():  # ✅ Хорошо
    pass

def test_1():  # ❌ Плохо
    pass

# 2. AAA паттерн (Arrange-Act-Assert)
def test_product_price_calculation():
    # Arrange (Подготовка)
    product = Product(price=1000, discount=20)
    
    # Act (Действие)
    final_price = product.calculate_final_price()
    
    # Assert (Проверка)
    assert final_price == 800

# 3. Независимость тестов
@pytest.fixture
def clean_database():
    """Каждый тест получает чистую базу данных"""
    db = create_test_database()
    yield db
    db.cleanup()

# 4. Явные ожидания вместо sleep
def test_element_appears_after_action(page):
    page.click("#submit-button")
    # ✅ Хорошо:
    page.wait_for_selector(".success-message")
    # ❌ Плохо:
    time.sleep(5)
```

**Практическое задание (5 мин)**
Создать простой тестовый фреймворк для калькулятора

#### Занятие 3.3: Настройка рабочего окружения
**Видео-скрипт (25 минут):**

**Установка Python и виртуального окружения (8 мин)**
```bash
# ШАГ 1: Проверка установленного Python
python --version  # Должно быть 3.8+
pip --version

# ШАГ 2: Создание виртуального окружения
python -m venv automation_env

# ШАГ 3: Активация виртуального окружения
# Windows:
automation_env\Scripts\activate
# Linux/Mac:
source automation_env/bin/activate

# ШАГ 4: Проверка активации
which python  # Должно показать путь к виртуальному окружению
```

**Установка необходимых библиотек (7 мин)**
```bash
# Создание requirements.txt
cat > requirements.txt << EOF
pytest==7.4.3
playwright==1.40.0
pytest-playwright==0.4.3
requests==2.31.0
python-dotenv==1.0.0
EOF

# Установка зависимостей
pip install -r requirements.txt

# Установка браузеров для Playwright
playwright install
```

**Структура проекта (5 мин)**
```
СТРУКТУРА ТЕСТОВОГО ПРОЕКТА:

automation_project/
├── tests/                  # Тестовые файлы
│   ├── unit/              # Модульные тесты
│   ├── integration/       # Интеграционные тесты
│   └── ui/               # UI тесты
├── pages/                # Page Object классы
├── fixtures/             # Тестовые фикстуры
├── utils/                # Вспомогательные функции
├── config/               # Конфигурационные файлы
├── reports/              # Отчеты о тестировании
├── .env                  # Переменные окружения
├── conftest.py           # Глобальные настройки pytest
└── requirements.txt      # Зависимости
```

**IDE настройка (3 мин)**
```python
# Рекомендуемые расширения для VS Code:
extensions = [
    "ms-python.python",           # Python
    "ms-playwright.playwright",   # Playwright
    "littlefoxteam.vscode-python-test-adapter",  # Test Explorer
    "ms-python.flake8",           # Linter
    "ms-python.black-formatter"   # Formatter
]

# Настройки VS Code (.vscode/settings.json)
vscode_settings = {
    "python.defaultInterpreterPath": "./automation_env/bin/python",
    "python.testing.pytestEnabled": True,
    "python.testing.unittestEnabled": False,
    "playwright.reuseBrowser": True
}
```

**Практическая работа (2 мин)**
Создание и настройка тестового проекта

#### Занятие 3.4: Первый автоматизированный тест
**Видео-скрипт (22 минуты):**

**Написание первого теста с Playwright (10 мин)**
```python
# test_first_browser_test.py

import pytest
from playwright.sync_api import Page

def test_google_search(page: Page):
    """
    Первый тест: поиск в Google
    """
    # Открытие страницы
    page.goto("https://www.google.com")
    
    # Проверка заголовка
    assert "Google" in page.title()
    
    # Поиск элемента поиска
    search_box = page.locator("[name='q']")
    
    # Ввод текста поиска
    search_box.fill("Playwright automation testing")
    
    # Нажатие Enter
    search_box.press("Enter")
    
    # Ожидание результатов
    page.wait_for_selector("#search")
    
    # Проверка наличия результатов
    results_count = page.locator(".g").count()
    assert results_count > 0
    
    # Проверка, что в результатах есть Playwright
    first_result = page.locator(".g").first.locator("h3")
    assert "Playwright" in first_result.inner_text()

# Запуск теста:
# pytest test_first_browser_test.py -v --headed
```

**Анализ структуры теста (6 мин)**
```python
# РАЗБОР СТРУКТУРЫ ТЕСТА:

class TestStructureAnalysis:
    def __init__(self):
        self.components = {}
    
    def analyze_test_components(self, test_function):
        """
        Анализ компонентов теста
        """
        components = {
            # ARRANGE - Подготовка
            'setup': [
                'page.goto()',      # Открытие страницы
                'data preparation'   # Подготовка данных
            ],
            
            # ACT - Действие
            'action': [
                'page.locator().fill()',  # Взаимодействие с элементами
                'page.click()',           # Клик по элементу
                'page.press()'            # Нажатие клавиши
            ],
            
            # ASSERT - Проверка
            'verification': [
                'assert statements',      # Утверждения
                'page.wait_for_*',        # Ожидания
                'element checks'          # Проверки элементов
            ]
        }
        return components

# ПРИНЦИПЫ ХОРОШЕГО ТЕСТА:
principles = {
    'specific': 'Тест проверяет одну конкретную функцию',
    'independent': 'Тест не зависит от других тестов',
    'repeatable': 'Тест дает одинаковый результат при повторном запуске',
    'self_validating': 'Тест автоматически определяет успех/провал',
    'timely': 'Тест пишется своевременно'
}
```

**Отладка тестов (4 мин)**
```python
# МЕТОДЫ ОТЛАДКИ:

# 1. Запуск в headed режиме
# pytest test_example.py --headed

# 2. Пауза для отладки
def test_with_debug_pause(page):
    page.goto("https://example.com")
    page.pause()  # Останавливается для ручной отладки
    # Продолжить выполнение: Resume в Playwright Inspector

# 3. Скриншоты при ошибках
def test_with_screenshots(page):
    try:
        page.goto("https://example.com")
        # ... тестовая логика
    except Exception as e:
        page.screenshot(path="failure.png")
        raise e

# 4. Логирование
import logging
logging.basicConfig(level=logging.INFO)

def test_with_logging(page):
    logging.info("Начало теста")
    page.goto("https://example.com")
    logging.info("Страница загружена")
    # ... остальная логика
```

**Практическое задание (2 мин)**
Создать тест для проверки простого веб-сайта

### Неделя 2: Практическая реализация
#### Занятие 3.5: Работа с локаторами
#### Занятие 3.6: Фикстуры и настройка тестов
#### Занятие 3.7: Тестирование форм
#### Занятие 3.8: Обработка исключений в тестах

### Неделя 3: Продвинутые основы
#### Занятие 3.9: Параметризация тестов
#### Занятие 3.10: Маркировка и выборочное выполнение
#### Занятие 3.11: Генерация отчетов
#### Занятие 3.12: Финальный проект модуля

## 🏠 Домашние задания

### Домашнее задание 3.1 (Базовое)
**Настройка тестового окружения**
- Установить Python 3.11+
- Создать виртуальное окружение
- Установить pytest и Playwright
- Создать структуру тестового проекта
- Написать первый тест, открывающий любой веб-сайт

### Домашнее задание 3.2 (Среднее)
**Тестирование формы регистрации**
- Найти сайт с формой регистрации
- Создать тесты для различных сценариев:
  - Успешная регистрация
  - Регистрация с некорректными данными
  - Проверка валидации полей
- Использовать различные типы локаторов
- Добавить скриншоты при ошибках

### Домашнее задание 3.3 (Продвинутое)
**Комплексное тестирование веб-приложения**
- Выбрать простое веб-приложение (например, TODO-лист)
- Создать набор тестов:
  - Модульные тесты для бизнес-логики
  - UI тесты для пользовательского интерфейса
  - Интеграционные тесты
- Настроить генерацию отчетов
- Документировать процесс настройки

## 📊 Тест знаний

**Промежуточный тест по модулю 3:**
- 20 вопросов по автоматизации тестирования
- 3 практических задания по написанию тестов
- Время выполнения: 90 минут
- Проходной балл: 75%

## 🎯 Проект модуля

**"Автоматизация тестирования учебного приложения"**
Создать полноценный набор автоматизированных тестов для простого веб-приложения:

**Обязательные части:**
1. **Настройка окружения** - документация по установке
2. **Модульные тесты** - тестирование бизнес-логики (минимум 10 тестов)
3. **UI тесты** - тестирование пользовательского интерфейса (минимум 15 тестов)
4. **Конфигурация** - файлы настроек и фикстуры
5. **Отчетность** - генерация отчетов в нескольких форматах

**Требования:**
- Использование pytest как основного фреймворка
- Применение Page Object Pattern
- Наличие параметризованных тестов
- Обработка исключений и ошибок
- Чистая архитектура кода
- Подробная документация

**Срок сдачи:** Конец 3 недели

---
*Модуль 3 закладывает основы автоматизации и готовит к более сложным темам*