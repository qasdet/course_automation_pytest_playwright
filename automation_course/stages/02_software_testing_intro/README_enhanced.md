# Модуль 2: Введение в тестирование ПО

## 🎯 Цели модуля (2 недели / 8 занятий)

**По окончании модуля студент сможет:**
- Понимать основные концепции тестирования программного обеспечения
- Различать виды тестирования и их цели
- Составлять тест-кейсы и тест-планы
- Проводить ручное тестирование
- Документировать результаты тестирования
- Понимать жизненный цикл тестирования
- **Применять знания для автоматизации тестирования**
- **Понимать связь между ручным и автоматизированным тестированием**
- **Создавать качественную тестовую документацию для автоматизаторов**

## 👨‍🏫 Методические материалы для преподавателя

### Общие рекомендации по преподаванию модуля тестирования:

**🎯 Основной подход:**
- Связывать теорию с реальными примерами из практики
- Использовать интерактивные демонстрации
- Показывать как тестирование влияет на качество продукта
- Объяснять ROI (возврат инвестиций) от качественного тестирования
- **Подчеркивать связь с автоматизацией тестирования**
- **Демонстрировать, как ручные тесты становятся автоматизированными**

**📋 Необходимые материалы:**
- Примеры реальных баг-репортов
- Тест-планы и тест-кейсы из production
- Демонстрационные приложения для тестирования
- Доступ к баг-трекерам (Jira, Trello, или демо-версии)
- **Примеры автоматизированных тестов для сравнения**
- **Инструменты для автоматизации (Selenium, Playwright демо)**

**⏰ Структура занятий:**
- 15 мин: Разбор домашнего задания
- 25 мин: Новая теория с примерами
- 35 мин: Практические упражнения
- 10 мин: Перерыв
- 20 мин: Работа в группах
- 15 мин: Подведение итогов и домашнее задание

### 🔧 Практические инструменты для демонстраций:

**Для демонстрации типов тестирования:**
- Онлайн-калькуляторы (для функционального тестирования)
- Веб-сайты с формами (для тестирования UI)
- API playground (для API тестирования)
- **Демонстрация автоматизированных тестов в действии**

**Для показа багов:**
- Подготовленные примеры с преднамеренными ошибками
- Реальные баг-репорты из open-source проектов
- Демонстрация инструментов тестирования
- **Сравнение ручного и автоматического обнаружения багов**

## 🧪 Автоматизация тестирования: Введение

### Связь ручного и автоматизированного тестирования

```python
# Пример: Одна и та же функциональность - разные подходы

# РУЧНОЕ ТЕСТИРОВАНИЕ
def manual_test_login():
    """
    Ручной тест авторизации:
    1. Открыть браузер
    2. Перейти на страницу логина
    3. Ввести email: user@example.com
    4. Ввести пароль: password123
    5. Нажать кнопку "Войти"
    6. Проверить, что попали в личный кабинет
    7. Записать результат в отчет
    Время: ~2 минуты
    """
    pass

# АВТОМАТИЗИРОВАННОЕ ТЕСТИРОВАНИЕ
def automated_test_login():
    """
    Автоматизированный тест авторизации:
    """
    # Весь процесс в 5 строках кода
    driver = webdriver.Chrome()
    driver.get("https://example.com/login")
    driver.find_element(By.ID, "email").send_keys("user@example.com")
    driver.find_element(By.ID, "password").send_keys("password123")
    driver.find_element(By.ID, "login-btn").click()
    
    # Проверка результата
    assert "dashboard" in driver.current_url
    assert driver.find_element(By.CLASS_NAME, "welcome-message").is_displayed()
    
    driver.quit()
    # Время выполнения: ~5 секунд
    # Может выполняться 1000 раз без усталости

# ROI СРАВНЕНИЕ:
manual_time = 2 * 60  # 2 минуты в секундах
auto_time = 5         # 5 секунд
setup_time = 30 * 60  # 30 минут на написание теста
runs_needed = 100     # Сколько раз будет запускаться

manual_total = manual_time * runs_needed  # 12,000 секунд (~3.3 часа)
auto_total = setup_time + (auto_time * runs_needed)  # 2,300 секунд (~38 минут)

savings = manual_total - auto_total  # 9,700 секунд (~2.7 часа экономии)
roi_percentage = (savings / setup_time) * 100  # 323% ROI
```

### Когда автоматизировать: Практическое руководство

```python
# Критерии для принятия решения об автоматизации

class AutomationDecisionMatrix:
    def __init__(self):
        self.positive_factors = []
        self.negative_factors = []
        self.score = 0
    
    def evaluate_test_case(self, scenario_description, frequency, business_critical, 
                          requires_human_judgment, ui_changes_frequently):
        """
        Оценка тест-кейса для автоматизации
        """
        
        # ЧАСТОТА ВЫПОЛНЕНИЯ (+10 за каждое совпадение)
        if frequency in ["daily", "every_commit", "multiple_times_per_day"]:
            self.positive_factors.append("Высокая частота выполнения")
            self.score += 10
        
        # КРИТИЧНОСТЬ (+15 за критическую функциональность)
        if business_critical:
            self.positive_factors.append("Критическая бизнес-функциональность")
            self.score += 15
            
        # ПОВТОРЯЕМОСТЬ (+8)
        if not requires_human_judgment:
            self.positive_factors.append("Может быть автоматизировано")
            self.score += 8
            
        # СТАБИЛЬНОСТЬ ИНТЕРФЕЙСА (+5)
        if not ui_changes_frequently:
            self.positive_factors.append("Стабильный интерфейс")
            self.score += 5
            
        # НЕГАТИВНЫЕ ФАКТОРЫ (-5 за каждый)
        if requires_human_judgment:
            self.negative_factors.append("Требует человеческого суждения")
            self.score -= 5
            
        if ui_changes_frequently:
            self.negative_factors.append("Интерфейс часто меняется")
            self.score -= 5
            
        # РЕКОМЕНДАЦИЯ
        if self.score >= 25:
            return "РЕКОМЕНДУЕТСЯ АВТОМАТИЗАЦИЯ"
        elif self.score >= 15:
            return "РАССМОТРЕТЬ ВОЗМОЖНОСТЬ АВТОМАТИЗАЦИИ"
        else:
            return "ЛЕГЧЕ ОСТАВИТЬ РУЧНЫМ ТЕСТИРОВАНИЕМ"

# Практические примеры:
examples = [
    {
        "scenario": "Тестирование API endpoint /users",
        "frequency": "every_commit",
        "business_critical": True,
        "requires_human_judgment": False,
        "ui_changes_frequently": False,
        "recommendation": "ОДНОЗНАЧНО АВТОМАТИЗИРОВАТЬ",
        "automation_tools": ["pytest", "requests", "Postman"]
    },
    {
        "scenario": "Тестирование нового UX дизайна",
        "frequency": "one_time",
        "business_critical": False,
        "requires_human_judgment": True,
        "ui_changes_frequently": True,
        "recommendation": "ОСТАВИТЬ РУЧНЫМ",
        "reason": "Требует визуальной оценки и интуиции"
    },
    {
        "scenario": "Регрессионное тестирование формы заказа",
        "frequency": "daily",
        "business_critical": True,
        "requires_human_judgment": False,
        "ui_changes_frequently": True,
        "recommendation": "АВТОМАТИЗИРОВАТЬ С ОСТОРОЖНОСТЬЮ",
        "strategy": "Автоматизировать стабильные части, ручное для новых элементов"
    }
]
```

## 📚 Теоретические основы тестирования

### Что такое тестирование ПО: Подробное объяснение

#### Определение и цели

**Тестирование ПО** - это процесс исследования программного продукта с целью выявления различий между ожидаемым и фактическим поведением, а также для оценки соответствия требованиям.

```python
# Пример: Что может пойти не так без тестирования?

class CalculatorWithoutTesting:
    def calculate_discount(self, price, discount_percent):
        # ОШИБКА: Не делится на 100!
        return price * discount_percent

# Тестирование выявляет ошибку:
class CalculatorWithTesting:
    def calculate_discount(self, price, discount_percent):
        if not (0 <= discount_percent <= 100):
            raise ValueError("Процент скидки должен быть от 0 до 100")
        return price * (1 - discount_percent / 100)

# Тесты:
def test_discount_calculation():
    calc = CalculatorWithTesting()
    
    # Правильные случаи
    assert calc.calculate_discount(1000, 20) == 800
    assert calc.calculate_discount(500, 0) == 500
    assert calc.calculate_discount(100, 100) == 0
    
    # Граничные случаи
    assert calc.calculate_discount(1000, 0) == 1000
    assert calc.calculate_discount(1000, 100) == 0
    
    # Ошибочные случаи
    try:
        calc.calculate_discount(1000, -10)
        assert False, "Должно быть исключение"
    except ValueError:
        pass  # Ожидаемое поведение

# Без тестирования эта ошибка могла бы попасть в production!
```

#### Принципы тестирования

**7 основных принципов тестирования:**

1. **Тестирование показывает наличие дефектов, а не их отсутствие**
   ```python
   # Даже если все тесты прошли, это не гарантирует отсутствия багов
   def test_user_registration():
       # Тесты могут проверить только те сценарии, которые мы предусмотрели
       # Реальные пользователи могут вести себя неожиданно
       pass
   ```

2. **Исчерпывающее тестирование невозможно**
   ```python
   # Невозможно протестировать все комбинации
   def complex_form(first_name, last_name, email, phone, address, city, country):
       # 10 полей × 100 вариантов каждого = 10^20 комбинаций!
       # Нужна стратегия тестирования, не исчерпывающий перебор
       pass
   ```

3. **Раннее тестирование экономит время и деньги**
   ```python
   # Сравнение стоимости исправления ошибок на разных этапах
   cost_comparison = {
       "Требования": 1,
       "Проектирование": 5,
       "Разработка": 10,
       "Тестирование": 20,
       "Production": 100
   }
   
   print("Стоимость исправления бага в production в 100 раз выше, чем на этапе требований!")
   ```

4. **Кластеризация дефектов**
   ```python
   # 80% ошибок обычно находятся в 20% модулей
   defect_distribution = {
       "user_auth_module": 25,    # 25 багов
       "payment_module": 20,      # 20 багов
       "dashboard": 15,           # 15 багов
       "other_modules": 40        # 40 багов в 20 разных модулях
   }
   # Фокусируемся на проблемных областях
   ```

5. **Пестицидный парадокс**
   ```python
   # Повторное выполнение одних и тех же тестов со временем становится менее эффективным
   def test_suite_evolution():
       # Версия 1.0 - тесты находят много багов
       # Версия 2.0 - те же тесты находят меньше багов
       # Нужно постоянно обновлять тесты!
       pass
   ```

6. **Тестирование зависит от контекста**
   ```python
   # Разные типы ПО требуют разных подходов
   testing_approaches = {
       "medical_software": "Очень строгое тестирование, регулируется законодательством",
       "game_app": "Фокус на юзабилити и производительность",
       "banking_system": "Безопасность и точность расчетов",
       "social_media": "Масштабируемость и обработка больших объемов данных"
   }
   ```

7. **Парадокс отсутствия ошибок**
   ```python
   # Даже без багов система может быть непригодной
   def perfect_but_useless_system():
       # Система работает без ошибок, но:
       # - Сложный интерфейс
       # - Медленная работа
       # - Не соответствует потребностям пользователей
       # Результат: никто не пользуется
       pass
   ```

### 🧪 Автоматизация в контексте принципов тестирования

```python
# Как принципы тестирования применяются в автоматизации:

class AutomationPrinciples:
    def __init__(self):
        self.principles_application = {}
    
    def early_automation(self):
        """Принцип раннего тестирования в автоматизации"""
        # Хорошо: Unit-тесты пишутся вместе с кодом
        def test_user_validation_unit():
            validator = UserValidator()
            assert validator.validate_email("valid@email.com") == True
            assert validator.validate_email("invalid-email") == False
        
        # Плохо: Автоматизация только после релиза
        # Это нарушает принцип раннего тестирования
        
    def automation_clustering(self):
        """Кластеризация дефектов в автоматизации"""
        # Фокус на автоматизацию критических модулей:
        critical_modules_for_automation = [
            "user_authentication",      # Много багов здесь
            "payment_processing",       # Бизнес-критично
            "data_validation_logic"     # Часто ломается
        ]
        
        # Менее критичные модули можно тестировать вручную
        manual_testing_modules = [
            "help_documentation",
            "marketing_pages",
            "static_content"
        ]
    
    def pesticide_paradox_solution(self):
        """Решение парадокса пестицида в автоматизации"""
        def maintain_test_suite():
            # Регулярное обновление тестов
            monthly_review_tasks = [
                "Обновить селекторы после изменений UI",
                "Добавить новые тест-кейсы для новых функций",
                "Удалить устаревшие тесты",
                "Пересмотреть приоритеты тестов"
            ]
            
            # Использование разных подходов
            testing_strategies = [
                "Позитивные сценарии",
                "Негативные сценарии", 
                "Граничные значения",
                "Случайные данные"
            ]

# Практическая реализация:
def create_robust_test_suite():
    """
    Создание устойчивого тестового набора с учетом всех принципов
    """
    
    # 1. Раннее тестирование - TDD подход
    def test_before_implementation():
        # Пишем тест до реализации функции
        def test_calculator_add():
            calc = Calculator()
            result = calc.add(2, 3)
            assert result == 5
    
    # 2. Фокус на критических областях
    def prioritize_critical_tests():
        critical_test_areas = {
            "security": ["auth_tests", "permission_tests"],
            "core_functionality": ["main_feature_tests"],
            "data_integrity": ["validation_tests", "persistence_tests"]
        }
        
    # 3. Разнообразие подходов
    def diverse_test_approaches():
        approaches = [
            "boundary_testing",      # Граничные значения
            "equivalence_classes",   # Классы эквивалентности
            "error_guessing",        # Предположение ошибок
            "exploratory_testing"    # Исследовательское (ручное)
        ]

# ROI от автоматизации с учетом принципов:
def calculate_automation_roi():
    factors = {
        "early_detection_savings": 80,      # % экономии от раннего обнаружения
        "repeatability_benefit": 90,        # % времени на повторные тесты
        "accuracy_improvement": 95,         # % улучшения точности
        "maintenance_costs": 20             # % затрат на поддержку
    }
    
    net_benefit = (
        factors["early_detection_savings"] +
        factors["repeatability_benefit"] +
        factors["accuracy_improvement"] -
        factors["maintenance_costs"]
    )
    
    return f"Чистая выгода от автоматизации: {net_benefit}%"
```

## ❓ Ответы на возможные вопросы студентов

### Вопросы по теории тестирования

**Q: Зачем нужно тестирование, если программист может протестировать свой код?**

A: Это отличный вопрос! Вот основные причины:

1. **Человеческий фактор:** Программист может не заметить свои собственные ошибки из-за психологической привязанности к коду
2. **Разные точки зрения:** Тестировщик смотрит на продукт глазами пользователя, а не разработчика
3. **Специализация:** Тестировщики знают специфические техники поиска багов
4. **Независимость:** Объективная проверка качества

**Для автоматизаторов особенно важно:**
```python
# Автоматизаторы создают "второй взгляд" на код
def automation_as_second_opinion():
    """
    Автоматизированные тесты как независимая проверка
    """
    # Разработчик пишет код
    def developer_implementation():
        return "code_with_potential_bugs"
    
    # Автоматизатор пишет тесты
    def automation_tests():
        # Проверяет то же самое, но другим путем
        return "independent_verification"
    
    # Результат: двойная проверка качества
```

**Q: Можно ли полностью автоматизировать все тестирование?**

A: Нет, и вот почему:

```python
# То, что нельзя автоматизировать:
impossible_to_automate = [
    "Творческое тестирование",           # Требует интуиции
    "Эмоциональное восприятие",          # Как пользователь чувствует интерфейс
    "Импровизация при тестировании",     # Исследовательские сценарии
    "Тестирование юзабилити",            # Субъективная оценка
    "Тестирование в нестандартных ситуациях"  # Непредсказуемые сценарии
]

# То, что лучше автоматизировать:
better_to_automate = [
    "Регрессионное тестирование",        # Повторяющиеся проверки
    "Тестирование API",                  # Структурированные запросы
    "Нагрузочное тестирование",          # Большое количество запросов
    "Тестирование производительности",   # Измерения времени
    "Тестирование безопасности"          # Проверка уязвимостей
]

# Гибридный подход - лучшее решение:
hybrid_approach = {
    "automated": {
        "smoke_tests": "Быстрая проверка основной функциональности",
        "regression_suite": "Полный набор регрессионных тестов",
        "api_tests": "Тестирование backend API",
        "performance_tests": "Нагрузочное тестирование"
    },
    "manual": {
        "ux_testing": "Проверка пользовательского опыта",
        "exploratory_testing": "Исследовательское тестирование",
        "accessibility_testing": "Тестирование доступности",
        "ad-hoc_testing": "Спонтанное тестирование"
    }
}
```

## 📋 Подробный тайминг занятий

### Занятие 2.1: Что такое тестирование ПО (90 минут)

**0-15 мин: Введение и проверка остаточных знаний**
- Краткий опрос по предыдущему модулю
- Введение в тему тестирования
- Цели и задачи занятия
- **Акцент на связь с автоматизацией**

**15-35 мин: Теория - Основные понятия**
- Определение тестирования
- Принципы тестирования
- Зачем нужно тестирование
- ROI от качественного тестирования
- **Сравнение ручного и автоматического тестирования**

**35-55 мин: Практика - Живая демонстрация**
- Демонстрация бага в реальном приложении
- Показ стоимости ошибок на разных этапах
- Примеры из практики
- **Демонстрация автоматизированного теста**

**55-70 мин: Интерактивная часть**
- Работа в группах: студенты ищут баги в подготовленном приложении
- Обсуждение найденных проблем
- Формулирование требований к тестированию
- **Обсуждение автоматизации найденных сценариев**

**70-90 мин: Закрепление и домашнее задание**
- Подведение итогов
- Ответы на вопросы
- Назначение домашнего задания
- **Анонс следующего занятия по автоматизации**

---
*Модуль 2 дает фундаментальные знания о тестировании, необходимые для дальнейшей автоматизации*