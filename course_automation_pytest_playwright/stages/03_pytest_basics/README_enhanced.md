# 03 — pytest: основы и лучшие практики

## 🎯 Цели этого этапа

- Освоить фундаментальные концепции фреймворка pytest
- Понять архитектуру и структуру тестов
- Научиться использовать фикстуры для управления тестовым окружением
- Овладеть техниками организации и запуска тестов
- Применять best practices написания качественных тестов

## 📚 Теоретические основы pytest

### Что такое pytest и почему он популярен?

**pytest** - это современный, мощный и гибкий фреймворк для тестирования Python кода. Его популярность обусловлена:

✅ **Простота использования** - интуитивный синтаксис, минимум boilerplate кода
✅ **Мощные фикстуры** - продвинутая система управления тестовым окружением
✅ **Богатая экосистема** - огромное количество плагинов и расширений
✅ **Отличная документация** - полное и понятное руководство
✅ **Активное сообщество** - быстрая помощь и развитие

### Архитектура pytest

```
Test Session
├── Collection Phase (Сбор тестов)
│   ├── Поиск файлов test_*.py
│   ├── Поиск функций test_*
│   └── Поиск классов Test*
├── Setup Phase (Настройка)
│   ├── Инициализация фикстур
│   └── Подготовка тестового окружения
├── Execution Phase (Выполнение)
│   ├── Запуск тестов
│   ├── Обработка assertions
│   └── Сбор результатов
└── Teardown Phase (Очистка)
    ├── Завершение фикстур
    └── Освобождение ресурсов
```

### Основные концепции

#### 1. Тестовые функции
```python
def test_something():  # Имя должно начинаться с "test_"
    # Arrange - Подготовка
    input_data = "hello"
    
    # Act - Действие
    result = input_data.upper()
    
    # Assert - Проверка
    assert result == "HELLO"
```

#### 2. Фикстуры
```python
import pytest

@pytest.fixture
def database_connection():
    # Setup
    conn = create_connection()
    yield conn  # Передаем значение тесту
    # Teardown
    conn.close()

def test_database_operation(database_connection):
    result = database_connection.query("SELECT * FROM users")
    assert len(result) > 0
```

#### 3. Параметризация
```python
@pytest.mark.parametrize("input,expected", [
    (2, 4),
    (3, 9),
    (4, 16),
])
def test_square(input, expected):
    assert square(input) == expected
```

## 🛠️ Подробное руководство по pytest

### Структура тестового проекта

```
my_project/
├── src/
│   └── calculator.py          # Код для тестирования
├── tests/
│   ├── __init__.py
│   ├── conftest.py           # Глобальные фикстуры
│   ├── test_calculator.py    # Тесты для calculator
│   ├── test_advanced.py      # Расширенные тесты
│   └── utils/
│       ├── __init__.py
│       └── test_helpers.py   # Вспомогательные тесты
├── pytest.ini               # Конфигурация pytest
└── requirements.txt
```

### Базовые команды pytest

```bash
# Запуск всех тестов
pytest

# Запуск с подробным выводом
pytest -v

# Запуск одного файла
pytest tests/test_calculator.py

# Запуск конкретного теста
pytest tests/test_calculator.py::test_addition

# Запуск тестов по маркеру
pytest -m slow

# Запуск тестов по имени
pytest -k "addition or subtraction"

# Остановка при первой ошибке
pytest -x

# Повтор последних неудачных тестов
pytest --lf

# Генерация отчета в HTML
pytest --html=report.html
```

### Расширенные возможности запуска

```bash
# Параллельный запуск
pytest -n 4  # 4 процесса

# Случайный порядок выполнения
pytest --random-order

# Покрытие кода
pytest --cov=src --cov-report=html

# Вывод print() в консоль
pytest -s

# Детальный traceback
pytest --tb=long

# Запуск в режиме отладки
pytest --pdb
```

## 🧪 Практические примеры

### Пример 1: Базовое тестирование функций

```python
# calculator.py
def add(a, b):
    """Сложение двух чисел"""
    return a + b

def divide(a, b):
    """Деление двух чисел"""
    if b == 0:
        raise ValueError("Division by zero")
    return a / b

def is_even(number):
    """Проверка четности числа"""
    return number % 2 == 0
```

```python
# test_calculator.py
import pytest
from src.calculator import add, divide, is_even

class TestCalculator:
    """Тесты для калькулятора"""
    
    def test_add_positive_numbers(self):
        """Тест сложения положительных чисел"""
        assert add(2, 3) == 5
        assert add(10, 20) == 30
    
    def test_add_negative_numbers(self):
        """Тест сложения отрицательных чисел"""
        assert add(-1, -1) == -2
        assert add(-5, 3) == -2
    
    def test_add_zero(self):
        """Тест сложения с нулем"""
        assert add(5, 0) == 5
        assert add(0, 0) == 0
    
    def test_divide_normal(self):
        """Тест нормального деления"""
        assert divide(10, 2) == 5.0
        assert divide(7, 2) == 3.5
    
    def test_divide_by_zero(self):
        """Тест деления на ноль"""
        with pytest.raises(ValueError, match="Division by zero"):
            divide(10, 0)
    
    @pytest.mark.parametrize("number,expected", [
        (2, True),
        (3, False),
        (0, True),
        (-2, True),
        (-3, False),
    ])
    def test_is_even(self, number, expected):
        """Параметризованный тест четности"""
        assert is_even(number) == expected
```

### Пример 2: Работа с фикстурами

```python
# conftest.py
import pytest
import tempfile
import json
from unittest.mock import Mock

@pytest.fixture
def temp_file():
    """Фикстура для временного файла"""
    with tempfile.NamedTemporaryFile(mode='w', delete=False) as f:
        f.write("temporary data")
        temp_path = f.name
    yield temp_path
    # Cleanup
    import os
    try:
        os.unlink(temp_path)
    except OSError:
        pass

@pytest.fixture
def sample_data():
    """Фикстура с тестовыми данными"""
    return {
        "users": [
            {"id": 1, "name": "Alice", "email": "alice@example.com"},
            {"id": 2, "name": "Bob", "email": "bob@example.com"}
        ],
        "settings": {
            "timeout": 30,
            "retries": 3
        }
    }

@pytest.fixture
def mock_api_client():
    """Фикстура с моком API клиента"""
    client = Mock()
    client.get_user.return_value = {"id": 1, "name": "Alice"}
    client.create_user.return_value = {"id": 3, "name": "Charlie"}
    return client
```

```python
# test_with_fixtures.py
import pytest
import json
import os

def test_temp_file_creation(temp_file):
    """Тест создания временного файла"""
    assert os.path.exists(temp_file)
    with open(temp_file, 'r') as f:
        content = f.read()
    assert content == "temporary data"

def test_sample_data_processing(sample_data):
    """Тест обработки тестовых данных"""
    users = sample_data["users"]
    assert len(users) == 2
    assert users[0]["name"] == "Alice"

def test_mock_api_client(mock_api_client):
    """Тест работы с моком API"""
    user = mock_api_client.get_user(1)
    assert user["name"] == "Alice"
    mock_api_client.get_user.assert_called_once_with(1)
```

### Пример 3: Маркировка и организация тестов

```python
# test_marked_tests.py
import pytest
import time

@pytest.mark.fast
def test_quick_calculation():
    """Быстрый тест"""
    assert 2 + 2 == 4

@pytest.mark.slow
def test_time_consuming_operation():
    """Медленный тест"""
    time.sleep(1)
    assert True

@pytest.mark.integration
def test_integration_scenario():
    """Интеграционный тест"""
    # Тест взаимодействия компонентов
    pass

@pytest.mark.skip(reason="Feature not implemented yet")
def test_future_feature():
    """Тест будущей функциональности"""
    pass

@pytest.mark.xfail(reason="Known issue", raises=ValueError)
def test_known_bug():
    """Тест известного бага"""
    raise ValueError("This is expected to fail")

class TestDatabase:
    @pytest.mark.database
    def test_connection(self):
        """Тест подключения к БД"""
        pass
    
    @pytest.mark.database
    @pytest.mark.slow
    def test_large_query(self):
        """Тест большого запроса"""
        pass
```

## 🤔 Часто задаваемые вопросы (FAQ)

### **Q: В чем разница между unittest и pytest?**
**A:** 
- **unittest**: Встроенный в Python, более формальный, требует наследования от TestCase
- **pytest**: Более гибкий, меньше boilerplate кода, мощные фикстуры, богатая экосистема

### **Q: Когда использовать фикстуры, а когда setUp/tearDown?**
**A:** Фикстуры pytest более мощные и гибкие. Используйте их всегда, кроме случаев миграции с unittest.

### **Q: Как правильно организовать тесты в больших проектах?**
**A:** Используйте паттерн "тестовая пирамида": много unit тестов, меньше integration, мало E2E.

### **Q: Что делать с flaky тестами?**
**A:** Используйте `pytest-rerunfailures` плагин или перепишите тест для большей стабильности.

## ✅ Чеклист качественных тестов

### Структура тестов
- [ ] Используется паттерн AAA (Arrange-Act-Assert)
- [ ] Каждый тест проверяет одну вещь
- [ ] Тесты независимы друг от друга
- [ ] Имена тестов описывают проверяемое поведение

### Реализация
- [ ] Используются осмысленные assertions
- [ ] Тестовые данные изолированы
- [ ] Фикстуры используются правильно
- [ ] Избегаются моки там, где не нужны

### Организация
- [ ] Тесты сгруппированы логически
- [ ] Используются подходящие маркеры
- [ ] Есть документация к сложным тестам
- [ ] Поддерживаются best practices

## 💡 Практическое задание

### Часть 1: Базовые тесты
1. Создайте модуль `string_utils.py` с функциями:
   - `reverse_string(s)` - переворачивает строку
   - `count_vowels(s)` - считает гласные буквы
   - `is_palindrome(s)` - проверяет палиндром

2. Напишите полное покрытие тестами в `test_string_utils.py`

### Часть 2: Работа с фикстурами
1. Создайте фикстуру для временной базы данных
2. Создайте фикстуру для mock HTTP клиента
3. Напишите тесты, использующие эти фикстуры

### Часть 3: Расширенные возможности
1. Используйте параметризацию для тестов
2. Добавьте маркеры slow/fast
3. Настройте pytest.ini с базовой конфигурацией

## ⚠️ Распространенные ошибки и решения

| Ошибка | Причина | Решение |
|--------|---------|---------|
| " fixture '...' not found" | Фикстура не определена | Проверьте conftest.py и импорты |
| Тесты зависают | Бесконечный цикл или deadlock | Добавьте таймауты, используйте --timeout |
| "AssertionError" без деталей | Непонятные assertions | Используйте осмысленные сообщения в assert |
| Медленные тесты | Неэффективные фикстуры | Оптимизируйте scope фикстур |

## 📚 Полезные ресурсы

### Документация:
- [Официальная документация pytest](https://docs.pytest.org/)
- [pytest plugins directory](https://plugincompat.herokuapp.com/)

### Полезные плагины:
- `pytest-cov` - покрытие кода
- `pytest-html` - HTML отчеты
- `pytest-xdist` - параллельный запуск
- `pytest-mock` - расширенные моки
- `pytest-timeout` - таймауты для тестов

### Best Practices:
- Пишите тесты до кода (TDD)
- Делайте тесты читаемыми как документацию
- Избегайте тестов, зависящих от внешних сервисов
- Поддерживайте тесты в актуальном состоянии