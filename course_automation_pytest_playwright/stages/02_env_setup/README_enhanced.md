# 02 — Подробная настройка окружения

## 🎯 Цели этого этапа

- Освоить профессиональную настройку рабочего окружения для автоматизации тестирования
- Понять принципы изоляции зависимостей и управления проектами
- Научиться решать типичные проблемы установки и конфигурации
- Подготовить production-ready окружение для дальнейшей работы

## 📚 Теоретические основы

### Виртуальные окружения Python

**Зачем нужны виртуальные окружения:**
- Изоляция зависимостей разных проектов
- Предотвращение конфликтов версий пакетов
- Обеспечение воспроизводимости сборки
- Упрощение деплоя и совместной работы

**Как работают виртуальные окружения:**
- Создают изолированную копию интерпретатора Python
- Имеют собственный каталог для пакетов (`site-packages`)
- Модифицируют переменные окружения `PATH` и `PYTHONPATH`
- Не влияют на системный Python

### Управление зависимостями

**requirements.txt vs setup.py vs pyproject.toml:**
- `requirements.txt` - простой список зависимостей
- `setup.py` - для пакетов, поддерживает metadata
- `pyproject.toml` - современный стандарт, поддерживает build системы

**Best practices:**
- Фиксируйте точные версии для production
- Используйте диапазоны версий для разработки
- Разделяйте зависимости на группы (dev, test, prod)

## 🛠️ Пошаговая настройка

### Шаг 1: Подготовка системы

#### Проверка установленного ПО
```bash
# Проверка Python
python --version
python3 --version

# Проверка pip
pip --version

# Проверка системных зависимостей (Linux)
sudo apt update
sudo apt install -y build-essential libssl-dev libffi-dev python3-dev

# Windows: убедитесь, что установлены Visual Studio Build Tools
```

#### Выбор директории проекта
```bash
# Создание рабочей директории
mkdir automation-project
cd automation-project

# Инициализация Git репозитория (рекомендуется)
git init
```

### Шаг 2: Создание виртуального окружения

#### Метод 1: Стандартный подход
```bash
# Создание окружения
python -m venv .venv

# Активация (Windows PowerShell)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
.\.venv\Scripts\Activate.ps1

# Активация (Windows Command Prompt)
.\.venv\Scripts\activate.bat

# Активация (Linux/Mac)
source .venv/bin/activate

# Проверка активации
which python  # должен указывать на .venv/bin/python
```

#### Метод 2: Продвинутая настройка
```bash
# Создание окружения с системными пакетами (если нужно)
python -m venv .venv --system-site-packages

# Создание окружения без pip (редко используется)
python -m venv .venv --without-pip

# Использование конкретной версии Python
/usr/bin/python3.11 -m venv .venv  # Linux
py -3.11 -m venv .venv             # Windows
```

### Шаг 3: Установка и обновление инструментов

#### Базовая настройка
```bash
# Обновление pip
python -m pip install --upgrade pip

# Установка setuptools и wheel
pip install --upgrade setuptools wheel

# Проверка установки
pip list
```

#### Установка основных зависимостей
```bash
# Установка из requirements.txt
pip install -r requirements.txt

# Установка с verbose выводом для отладки
pip install -r requirements.txt -v

# Установка в режиме разработки (editable)
pip install -e .
```

### Шаг 4: Настройка Playwright

#### Установка Playwright
```bash
# Установка пакета
pip install playwright

# Установка браузеров
playwright install

# Установка с системными зависимостями (Linux)
playwright install --with-deps

# Установка конкретных браузеров
playwright install chromium firefox webkit
```

#### Проверка установки
```bash
# Проверка версии
playwright --version

# Проверка установленных браузеров
playwright install --check

# Запуск теста
python -c "
from playwright.sync_api import sync_playwright
with sync_playwright() as p:
    browser = p.chromium.launch()
    page = browser.new_page()
    page.goto('https://example.com')
    print(page.title())
    browser.close()
"
```

### Шаг 5: Docker настройка

#### Локальная сборка образа
```bash
# Сборка образа
docker build -t automation-test-env .

# Запуск контейнера
docker run -it --rm automation-test-env bash

# Запуск тестов в контейнере
docker run --rm automation-test-env pytest

# Монтирование локальной директории
docker run -v $(pwd):/app -it automation-test-env bash
```

#### Проверка Docker окружения
```bash
# Проверка доступности X11 (для GUI приложений)
docker run -it --rm -e DISPLAY=$DISPLAY -v /tmp/.X11-unix:/tmp/.X11-unix automation-test-env

# Проверка браузеров в контейнере
docker run --rm automation-test-env playwright install --check
```

## 🤔 Часто задаваемые вопросы (FAQ)

### **Q: Какой метод активации виртуального окружения самый надежный?**
**A:** Для Windows PowerShell используйте `.\.venv\Scripts\Activate.ps1`, для Linux/Mac - `source .venv/bin/activate`. Всегда проверяйте, что `which python` указывает на ваше виртуальное окружение.

### **Q: Что делать, если pip install падает с ошибками компиляции?**
**A:** Установите системные зависимости:
- **Ubuntu/Debian:** `sudo apt install build-essential python3-dev`
- **CentOS/RHEL:** `sudo yum groupinstall "Development Tools"`
- **Windows:** Установите Visual Studio Build Tools

### **Q: Почему Playwright не может установить браузеры?**
**A:** Проверьте:
1. Достаточно ли места на диске (требуется ~2GB)
2. Права доступа к директории установки
3. Наличие интернет-соединения
4. На Linux: установите системные зависимости из Dockerfile

### **Q: Нужно ли использовать Docker для локальной разработки?**
**A:** Для начала нет. Используйте локальное окружение для разработки, Docker - для CI/CD и воспроизводимости тестов.

## ✅ Чеклист настройки

### Обязательные проверки:
- [ ] Python 3.10+ установлен и доступен
- [ ] Виртуальное окружение создано и активировано
- [ ] pip обновлен до последней версии
- [ ] Все зависимости из requirements.txt установлены
- [ ] Playwright и браузеры установлены успешно
- [ ] Простой тест проходит успешно
- [ ] Docker образ собирается без ошибок

### Дополнительные проверки:
- [ ] IDE правильно настроена для виртуального окружения
- [ ] Git репозиторий инициализирован
- [ ] .gitignore содержит .venv и __pycache__
- [ ] requirements.txt содержит все необходимые зависимости
- [ ] Документация по настройке окружения создана

## 💡 Практическое задание

### Часть 1: Базовая настройка
1. Создайте новую директорию для проекта
2. Настройте виртуальное окружение
3. Установите все зависимости
4. Проверьте работу Playwright
5. Зафиксируйте результаты в `env_check.md`

### Часть 2: Продвинутая настройка
1. Настройте IDE для работы с виртуальным окружением
2. Создайте скрипт автоматической настройки окружения
3. Настройте Docker для локальной разработки
4. Протестируйте воспроизводимость окружения

### Часть 3: Документирование
Создайте файл `setup_guide.md` с:
- Пошаговой инструкцией по настройке
- Решениями возможных проблем
- Скриптами для автоматизации

## ⚠️ Возможные проблемы и решения

| Проблема | Решение | Команда |
|----------|---------|---------|
| "No module named venv" | Установите python3-venv | `sudo apt install python3-venv` |
| "Permission denied" при активации | Измените политику PowerShell | `Set-ExecutionPolicy RemoteSigned` |
| Ошибки компиляции при pip install | Установите build tools | `sudo apt install build-essential` |
| Playwright не может запустить браузер | Установите системные зависимости | Смотрите Dockerfile |
| Docker build fails | Проверьте Dockerfile и права | `docker build --no-cache .` |

## 📚 Полезные ресурсы

### Документация:
- [Python Virtual Environments](https://docs.python.org/3/tutorial/venv.html)
- [pip Documentation](https://pip.pypa.io/en/stable/)
- [Playwright Installation](https://playwright.dev/python/docs/intro)
- [Docker Documentation](https://docs.docker.com/)

### Инструменты:
- **pyenv** - управление версиями Python
- **pipenv** - управление зависимостями и виртуальными окружениями
- **poetry** - современная система управления зависимостями
- **conda** - для научных проектов и data science

### Best Practices:
- Всегда используйте виртуальные окружения
- Фиксируйте версии зависимостей для production
- Используйте .gitignore для исключения служебных файлов
- Документируйте процесс настройки окружения