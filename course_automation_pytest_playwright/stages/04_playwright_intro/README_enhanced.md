# 04 — Введение в Playwright (Python)

## 🎯 Цели этого этапа

- Освоить фундаментальные концепции Playwright и его архитектуру
- Научиться эффективно работать с локаторами и селекторами
- Понять принципы автоматического ожидания и обработки асинхронности
- Овладеть базовыми методами навигации и взаимодействия с элементами
- Научиться писать стабильные и надежные UI тесты

## 📚 Теоретические основы Playwright

### Что такое Playwright и почему он лучший выбор?

**Playwright** - это современный фреймворк для автоматизации браузеров, созданный Microsoft, который предоставляет единый API для управления Chromium, Firefox и WebKit.

#### Основные преимущества Playwright:

✅ **Автоматическое ожидание** - элементы автоматически ждут своей готовности
✅ **Мощные селекторы** - поддержка CSS, XPath, текстовых и визуальных селекторов
✅ **Multi-tab и iframe поддержка** - полноценная работа со сложными сценариями
✅ **Mobile эмуляция** - тестирование мобильных версий сайтов
✅ **Network interception** - контроль и модификация сетевых запросов
✅ **Cross-browser поддержка** - единый API для всех браузеров
✅ **Отличная документация** - полное и понятное руководство

### Архитектура Playwright

```
Playwright Application
├── BrowserType (chromium, firefox, webkit)
│   ├── Browser (экземпляр браузера)
│   │   ├── BrowserContext (изолированный контекст)
│   │   │   ├── Page (вкладка/страница)
│   │   │   │   ├── Frame (фреймы внутри страницы)
│   │   │   │   ├── Locator (локаторы элементов)
│   │   │   │   └── Request/Response (сетевые запросы)
│   │   │   └── Storage (cookies, localStorage)
│   │   └── Page (дополнительные страницы)
│   └── DeviceDescriptors (мобильные устройства)
└── Selectors Engine (движок селекторов)
```

### Основные компоненты:

1. **Browser** - экземпляр браузера (Chrome, Firefox, Safari)
2. **BrowserContext** - изолированный контекст с собственными cookies и настройками
3. **Page** - вкладка браузера или страница
4. **Locator** - интеллектуальный селектор для поиска элементов
5. **Frame** - iframe или frame внутри страницы

## 🛠️ Подробное руководство по Playwright

### Установка и настройка

```bash
# Установка Playwright
pip install playwright

# Установка браузеров
playwright install

# Установка с системными зависимостями (Linux)
playwright install --with-deps

# Установка конкретных браузеров
playwright install chromium firefox webkit
```

### Базовые операции

#### Запуск браузера

```python
from playwright.sync_api import sync_playwright

# Базовый запуск
with sync_playwright() as p:
    browser = p.chromium.launch()
    page = browser.new_page()
    page.goto("https://example.com")
    print(page.title())
    browser.close()

# Запуск с параметрами
with sync_playwright() as p:
    browser = p.chromium.launch(
        headless=False,        # Видимый браузер
        slow_mo=1000,         # Замедление на 1 секунду
        devtools=True         # Открыть devtools
    )
    page = browser.new_page()
    # ... ваш код ...
    browser.close()
```

#### Работа с контекстами

```python
# Изолированные контексты
with sync_playwright() as p:
    browser = p.chromium.launch()
    
    # Контекст 1 - обычный пользователь
    context1 = browser.new_context()
    page1 = context1.new_page()
    page1.goto("https://example.com")
    
    # Контекст 2 - другой пользователь
    context2 = browser.new_context(
        viewport={"width": 1280, "height": 720},
        user_agent="Custom User Agent",
        locale="en-US"
    )
    page2 = context2.new_page()
    page2.goto("https://example.com")
    
    browser.close()
```

## 🎯 Локаторы и селекторы

### Типы селекторов

#### 1. CSS селекторы (рекомендуемые)
```python
# По ID
page.locator("#submit-button")

# По классу
page.locator(".btn-primary")

# По атрибуту
page.locator("[data-testid='login-form']")

# По тегу и классу
page.locator("button.btn-submit")

# Комбинированные селекторы
page.locator("form.login input[type='email']")
```

#### 2. Текстовые селекторы
```python
# Точный текст
page.locator("text=Submit Form")

# Частичный текст
page.locator("text=Submit")

# Регистрозависимый текст
page.locator("text='SUBMIT FORM'")

# Внутри конкретного элемента
page.locator("nav >> text=Home")
```

#### 3. XPath селекторы
```python
# Классический XPath
page.locator("xpath=//button[@class='submit']")

# Сокращенная запись
page.locator("//input[@type='password']")

# Относительный XPath
page.locator("//form//input[1]")
```

#### 4. Визуальные селекторы (экспериментальные)
```python
# По изображению
page.locator("img=logo.png")

# По скриншоту
page.locator("screenshot=button.png")
```

### Продвинутые техники локаторов

#### Chain локаторы
```python
# Поиск внутри элемента
form = page.locator("form.login")
email_input = form.locator("input[type='email']")
password_input = form.locator("input[type='password']")

# Несколько условий
page.locator("button").filter(has_text="Submit").filter(visible=True)

# Поиск по позиции
page.locator("li").nth(0)  # Первый элемент
page.locator("tr").last     # Последний элемент
```

#### Локаторы с условиями
```python
# Элементы с определенным текстом
page.locator("div").filter(has_text="Important")

# Видимые элементы
page.locator("button").filter(visible=True)

# Активные элементы
page.locator("input").filter(enabled=True)

# Выбранные элементы
page.locator("option").filter(selected=True)
```

## 🔄 Навигация и ожидание

### Навигация по страницам

```python
# Базовая навигация
page.goto("https://example.com")

# Навигация с ожиданием
page.goto("https://example.com", wait_until="networkidle")

# Навигация с таймаутом
page.goto("https://slow-site.com", timeout=30000)

# Обновление страницы
page.reload()

# Назад/вперед
page.go_back()
page.go_forward()
```

### Автоматическое ожидание

```python
# Playwright автоматически ждет:
# - Загрузки DOM
# - Загрузки сетевых ресурсов
#- Выполнения JavaScript
# - Отображения элементов

# Явное ожидание элемента
page.wait_for_selector("button.submit")

# Ожидание URL
page.wait_for_url("**/dashboard")

# Ожидание заголовка
page.wait_for_function("document.title.includes('Dashboard')")
```

### Ручное управление ожиданиями

```python
# Таймауты для конкретных операций
page.set_default_timeout(30000)  # 30 секунд по умолчанию

# Установка таймаута для элемента
button = page.locator("button.submit")
button.click(timeout=10000)  # 10 секунд

# Пользовательские ожидания
page.wait_for_function("""
    () => document.querySelector('.loading').style.display === 'none'
""")
```

## 🖱️ Взаимодействие с элементами

### Клик по элементам

```python
# Простой клик
page.locator("button").click()

# Клик с ожиданием
page.locator("button").click(force=True)

# Клик по координатам
page.locator("div").click(position={"x": 10, "y": 15})

# Двойной клик
page.locator("button").dblclick()

# Клик правой кнопкой
page.locator("element").click(button="right")
```

### Работа с формами

```python
# Ввод текста
page.locator("input[name='username']").fill("john_doe")

# Очистка и ввод
page.locator("input").clear()
page.locator("input").fill("new text")

# Ввод с эмуляцией печати
page.locator("textarea").type("Hello World", delay=100)

# Выбор из dropdown
page.locator("select").select_option("option2")
page.locator("select").select_option(value="2")
page.locator("select").select_option(label="Option Two")

# Checkbox и radio buttons
page.locator("input[type='checkbox']").check()
page.locator("input[type='radio']").check()
```

### Работа с файлами

```python
# Загрузка файлов
page.locator("input[type='file']").set_input_files("path/to/file.pdf")

# Множественная загрузка
page.locator("input[type='file']").set_input_files([
    "file1.jpg",
    "file2.png"
])

# Ожидание скачивания
with page.expect_download() as download_info:
    page.locator("button.download").click()
download = download_info.value
download.save_as("downloaded_file.pdf")
```

## 🧪 Практические примеры

### Пример 1: Тестирование формы логина

```python
import pytest
from playwright.sync_api import Page

def test_login_form(page: Page):
    """Тест формы логина"""
    
    # ARRANGE - Подготовка
    page.goto("https://example-login-site.com/login")
    
    # ACT - Действия
    page.locator("input[name='username']").fill("testuser")
    page.locator("input[name='password']").fill("password123")
    page.locator("button[type='submit']").click()
    
    # ASSERT - Проверки
    page.wait_for_url("**/dashboard")
    assert "Dashboard" in page.title()
    assert page.locator(".welcome-message").is_visible()

def test_invalid_login(page: Page):
    """Тест неверных учетных данных"""
    
    page.goto("https://example-login-site.com/login")
    
    page.locator("input[name='username']").fill("wronguser")
    page.locator("input[name='password']").fill("wrongpass")
    page.locator("button[type='submit']").click()
    
    # Проверка ошибки
    error_message = page.locator(".error-message")
    assert error_message.is_visible()
    assert "Invalid credentials" in error_message.text_content()
```

### Пример 2: Работа с динамическим контентом

```python
def test_dynamic_content(page: Page):
    """Тест динамически загружаемого контента"""
    
    page.goto("https://dynamic-content-site.com")
    
    # Ожидание появления элемента
    page.wait_for_selector(".dynamic-item")
    
    # Проверка количества элементов
    items_count = page.locator(".dynamic-item").count()
    assert items_count > 0
    
    # Взаимодействие с динамическими элементами
    first_item = page.locator(".dynamic-item").first
    first_item.click()
    
    # Ожидание изменения контента
    page.wait_for_selector(".modal.open")
    modal_text = page.locator(".modal-content").text_content()
    assert "Details" in modal_text
```

### Пример 3: Тестирование с мобильной эмуляцией

```python
@pytest.fixture
def mobile_page(page: Page):
    """Фикстура для мобильной страницы"""
    page.set_viewport_size({"width": 375, "height": 812})
    page.emulate_media(type="screen")
    return page

def test_mobile_navigation(mobile_page: Page):
    """Тест навигации на мобильном устройстве"""
    
    mobile_page.goto("https://responsive-site.com")
    
    # Открытие мобильного меню
    mobile_page.locator(".mobile-menu-toggle").click()
    
    # Проверка меню
    menu = mobile_page.locator(".mobile-menu")
    assert menu.is_visible()
    
    # Навигация по меню
    mobile_page.locator(".mobile-menu a[href='/about']").click()
    mobile_page.wait_for_url("**/about")
```

## 🤔 Часто задаваемые вопросы (FAQ)

### **Q: В чем разница между click() и force_click()?**
**A:** `click()` ожидает, что элемент станет кликабельным, `force_click()` кликает сразу, игнорируя состояние элемента.

### **Q: Как обрабатывать всплывающие окна (alerts)?**
**A:** 
```python
page.on("dialog", lambda dialog: dialog.accept())
# или
page.on("dialog", lambda dialog: dialog.dismiss())
```

### **Q: Как работать с iframe?**
**A:**
```python
iframe = page.frame_locator("iframe[name='content']")
iframe.locator("#button").click()
```

### **Q: Как сделать скриншот при ошибке?**
**A:**
```python
def test_with_screenshot(page: Page):
    try:
        # ваш тест
        pass
    except Exception as e:
        page.screenshot(path="failure.png", full_page=True)
        raise e
```

## ✅ Чеклист освоения Playwright

### Базовые навыки
- [ ] Могу запустить браузер и открыть страницу
- [ ] Понимаю концепцию локаторов и селекторов
- [ ] Могу взаимодействовать с базовыми элементами (кнопки, поля ввода)
- [ ] Использую автоматическое ожидание правильно

### Продвинутые навыки
- [ ] Работаю с динамическим контентом
- [ ] Использую chain локаторы и фильтры
- [ ] Обрабатываю всплывающие окна и alerts
- [ ] Работаю с iframe и множественными вкладками

### Профессиональные навыки
- [ ] Создаю надежные и стабильные тесты
- [ ] Использую mobile эмуляцию
- [ ] Обрабатываю сетевые запросы
- [ ] Настраиваю контексты для разных сценариев

## 💡 Практическое задание

### Часть 1: Базовое тестирование
1. Создайте тест для сайта `https://demoqa.com/`
2. Протестируйте форму регистрации
3. Проверьте работу различных элементов управления

### Часть 2: Продвинутые сценарии
1. Реализуйте тест с мобильной эмуляцией
2. Создайте тест с обработкой всплывающих окон
3. Напишите тест для работы с iframe

### Часть 3: Оптимизация
1. Реализуйте Page Object для тестируемого сайта
2. Создайте фикстуры для повторяющихся действий
3. Добавьте скриншоты при ошибках

## ⚠️ Распространенные ошибки и решения

| Ошибка | Причина | Решение |
|--------|---------|---------|
| "Element is not visible" | Элемент скрыт или еще не загрузился | Используйте автоматическое ожидание или `wait_for_selector()` |
| "Element is detached from DOM" | Элемент был удален со страницы | Найдите элемент заново или используйте более стабильные селекторы |
| "Timeout exceeded" | Долгая загрузка или сложный JavaScript | Увеличьте таймаут или используйте `wait_until` параметры |
| "Target closed" | Браузер закрылся преждевременно | Проверьте порядок закрытия ресурсов |

## 📚 Полезные ресурсы

### Документация:
- [Официальная документация Playwright](https://playwright.dev/python/)
- [Playwright Inspector](https://playwright.dev/python/docs/inspector)
- [Playwright Test Runner](https://playwright.dev/python/docs/test-runners)

### Инструменты:
- **Playwright Inspector** - для отладки селекторов
- **Playwright Codegen** - генерация кода по действиям
- **Playwright Trace Viewer** - анализ выполнения тестов

### Best Practices:
- Используйте текстовые селекторы вместо XPath когда возможно
- Создавайте надежные Page Object модели
- Добавляйте скриншоты и трассировки для отладки
- Тестируйте на разных браузерах и устройствах